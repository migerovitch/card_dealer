from card import Card

class CardDealer:
	"""
	A class for dealing and handling groups of cards
	"""
	def __init__():
		cards = []
		for i in range(3):
			for j in range(12):
				cards.insert(Card(i, j))